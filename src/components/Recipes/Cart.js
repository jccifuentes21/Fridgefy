import classes from "./Cart.module.css";
import Sidebar from "../UI/Sidebar";
import RecipeCartItem from "./RecipeCartItem";

const Cart = () => {
  return( 
  <Sidebar title="Cart">
    <ul>
      {/* <RecipeCartItem key={} id={} title={} image={} /> */}
    </ul>
  </Sidebar>

  );
};

export default Cart;
